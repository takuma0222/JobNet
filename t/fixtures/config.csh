#!/bin/csh
# Configuration file loaded via source
# Variables defined here should be available in the parent script

setenv BATCH_DIR /opt/batch
setenv LIB_DIR ${BATCH_DIR}/lib
setenv COMMON_SCRIPT ${LIB_DIR}/common.sh
